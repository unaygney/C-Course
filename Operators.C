#include <stdio.h>

int main()
{ 

    int x = 9;
    int y = 15;
    // int result = x % y;

    // printf("%d", result);

    /*

        Comparision Operators

        == != > < <= >=


        Logical Operators

        && || !
    */

    // printf("%d", x > 10 && x > 20 || x > 12);

    if (x > 10)
    {
        printf("X 10'dan buyuktur");
    }
    else
    {
        printf("x 10'dan kucuktur");
    };
    return 0;
}