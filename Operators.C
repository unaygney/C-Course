#include <stdio.h>

int main()
{

    int x = 15;
    int y = 15;
    // int result = x % y;

    // printf("%d", result);

    /*

        Comparision Operators

        == != > < <= >=


        Logical Operators

        && || !
    */

    printf("%d", x > 10 && x > 20 || x > 12);
    return 0;
}