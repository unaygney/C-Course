#include <stdio.h>

int main()
{

    /*
    int (4bytes) | ( %d )
    char (1bytes) | (%c)
    float (4 bytes) | (%f)
    double (4 bytes) | (%lf)

    */

    int number = 10;
    float number2 = 5.56;

    printf(" Birinci Sayim : %d 2.ci Sayim %f", number , number2);

    return 0;
}